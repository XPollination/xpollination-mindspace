# Changelog: pm-status-branch-versioning-checks v0.0.6

## Changes from v0.0.5

- **Removed skill split**: Eliminated pm.scan/pm.drill split recommendation per LIAISON evaluation (saves nothing, adds complexity).
- **Corrected prod port**: PROD deployment now references port 8080 (actual), not 4100 (planned). Added port migration steps as future work.
- **Preserved**: All v0.0.1-v0.0.4 changes (verification checks, visual hierarchy, style guide, HUMAN APPROVAL, AskUserQuestion, deployment detection).
