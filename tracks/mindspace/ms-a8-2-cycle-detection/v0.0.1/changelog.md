# Changelog: ms-a8-2-cycle-detection

## v0.0.1 — Initial Design

- PDSA design for cycle detection on dependency creation
- DFS-based cycle detection service with path reporting
- Integrates into existing POST /dependencies endpoint
- 2 files: cycle-detection.ts (NEW), task-dependencies.ts (UPDATE)
- 8 test cases
