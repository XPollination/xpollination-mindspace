# Changelog: Changelog Quality Gate — v0.0.2

## Changes
- Added changelog_ref quality gate on review→complete for design tasks (has pdsa_ref)
- Gate rejects missing or empty changelog_ref with clear error message
- Changelog.md must live in version directory root (tracks/<domain>/<slug>/v0.0.N/changelog.md)
- Added changelog_ref as clickable link in viz detail panel (amber styling)
- Added changelog_ref to PM status WHAT WAS DONE template
- Forward-looking only — existing completed tasks unaffected

## Date
2026-03-04
