{
  "version": "v0.0.6",
  "title": "Dark Mode Regression Fix",
  "date": "2026-03-04",
  "changes": [
    "Fix: liaison mode select renders correctly in light mode (removed inline dark styles)",
    "Fix: version history button renders correctly in light mode (removed inline dark styles)",
    "Fix: approval actions divider respects light/dark mode",
    "Removed !important overrides — proper CSS specificity via class selectors"
  ]
}
