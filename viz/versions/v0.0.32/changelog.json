{
  "version": "v0.0.32",
  "title": "Fix: API key regenerate reads user_id from JWT",
  "date": "2026-03-17",
  "tasks": [
    "ms-viz-data-proxy-conflict",
    "ms-version-bump-enforcement",
    "ms-settings-page-bugs"
  ],
  "changes": [
    "POST /api/keys reads user_id from JWT instead of requiring it in body",
    "Settings regenerate button works for logged-in users"
  ]
}
