/**
 * A2A Frontend Module — Theia extension for agent panel integration
 * Phase 2: will provide agent panel widgets, SSE stream management, task views
 */
import { ContainerModule } from '@theia/core/shared/inversify';

export default new ContainerModule(bind => {
  // Phase 2: bind A2A services, agent panel widget, SSE stream handler
  console.log('@mindspace/theia-a2a extension loaded');
});
